[package]
name = "mediary-core"
version = "0.1.0"
edition = "2024"

[dependencies]
# infer = "0.22.0"
# rusqlite = { version = "0.40.2", features = ["bundled"] }
# walkdir = "2.5.0"
strum = { version = "0.28", features = ["derive"] }
