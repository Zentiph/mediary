[package]
name = "mediary-core"
version = "0.1.0"
edition = "2024"

[dependencies]
directories = "6.0.0"
rusqlite = { version = "0.40.2", features = ["bundled"] }
strum = { version = "0.28", features = ["derive"] }

[dev-dependencies]
tempfile = "3.27.0"
# infer = "0.22.0"
# walkdir = "2.5.0"
